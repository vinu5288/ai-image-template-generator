services:
  - type: web
    name: ai-image-template-generator
    env: node
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    env: node
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_VERSION
        value: 18.x
      - key: VITE_GEMINI_API_KEY
        sync: false
    routes:
      - type: rewrite
        source: '/*'
        destination: '/index.html'
